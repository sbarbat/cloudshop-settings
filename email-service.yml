server:
    port: 10002
    
eureka:
    client:
        serviceUrl:
            defaultZone: ${DISCOVERY_SERVICE_URL:http://localhost:8761/eureka}
    
logging:
    level:
        org.springframework.cloud: 'DEBUG'

queue:
    host: localhost
    port: 5672